# Force apex domain (SEO: one canonical host)
https://www.tropesmith.com/*  https://tropesmith.com/:splat  301
https://tropesmith-site.pages.dev/*  https://tropesmith.com/:splat  301

# Cloudflare Pages _redirects
# Routes short links through the Supabase 'go' edge function for click tracking.
# Pattern: /go/<slug> → Supabase /functions/v1/go?slug=<slug> (302, logs click, then redirects to intake)
# Status 302 (proxy) so the URL bar still shows /go/<slug> when fetched, but the response is the 302 from Supabase.

/go/:slug   https://vsbytdonbuwrrlmwteaw.supabase.co/functions/v1/go?slug=:slug   302
/go         https://tropesmith.com/?utm_source=go_unknown                          302


# Trope Pulse public archive: DO NOT put a rule here either. The two status-200 rules that
# used to live on these lines never worked — CF Pages cannot 200-proxy to an external origin
# — so /pulse/<sub> 404'd from the day it shipped. IN-0778 replaced them with a same-origin
# Pages Function (functions/pulse/[[path]].js). _redirects is evaluated BEFORE Functions.

# lane-score: DO NOT put a redirect here. IN-0778 replaced the TS-0490 302 with a
# same-origin Pages Function (functions/lane-score/[[lane]].js) that fetches the
# Supabase edge fn server-side, so the shareable URL stays tropesmith.com. _redirects
# is evaluated BEFORE Functions, so re-adding a /lane-score/* rule here silently sends
# every shared link back off-site and re-breaks the link magnet (TS-0540 regression class).

# Renamed/removed lane (GSC 404 IN-0699)
/market/mystery-cozy-tropes/  /market/mystery-cozy/  301
